# Denario runtime dependencies
# Python >= 3.11 is required for full dependency compatibility.
# Note: `futurehouse-client` is intentionally excluded from the base set because
# some enterprise mirrors do not provide it. Install it separately only if you
# use FutureHouse idea-check features.
langchain-google-genai
langchain-anthropic==1.3.4
langchain-openai==1.1.11
langchain-core=1.2.5
langchain-nvidia-ai-endpoints
langgraph>=0.3.25
google-cloud-aiplatform
google-ai-generativelanguage>=0.6.17
flatbuffers>=24.12.23
filelock>=3.17.0
cmbagent>=0.0.1.post30
pymupdf
pillow
jsonschema
json
pip
denario
mistralai
